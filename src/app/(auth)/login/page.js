import React from 'react';
import Login from '@/pages/authentication/Login';

export const metadata = {
    title: "Oakmont Athletic : Login",
    description: "Oakmont Athletic Login"
};

const page = () => <Login/>

export default page